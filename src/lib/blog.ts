// Блог + новини. Дописи зберігаються тут (простий контент-масив — без CMS/БД).
// Додати допис = дописати обʼєкт у POSTS. Контент — абзаци (порожній рядок = новий
// абзац); рядок, що починається з "## " — підзаголовок; "- " — пункт списку.

export type PostCategory = "blog" | "news";

export interface Post {
  slug: string;
  title: string;
  excerpt: string;
  date: string;            // "YYYY-MM-DD"
  category: PostCategory;
  readingMinutes?: number;
  content: string;
}

export const CATEGORY_LABEL: Record<PostCategory, string> = {
  blog: "Стаття",
  news: "Новина",
};

const POSTS: Post[] = [
  {
    slug: "yak-obraty-avtocyvilku",
    title: "Як обрати автоцивілку та не переплатити",
    excerpt: "Коротко про те, від чого залежить ціна ОСЦПВ, як порівняти пропозиції страхових і на що звернути увагу перед оформленням.",
    date: "2026-08-01",
    category: "blog",
    readingMinutes: 4,
    content: `Автоцивілка (ОСЦПВ) — обовʼязкове страхування, тож базові умови однакові в усіх страхових. Але ціна відрізняється, і на неї впливає кілька факторів.

## Від чого залежить ціна
- регіон реєстрації авто;
- обʼєм двигуна та тип транспортного засобу;
- наявність пільг у власника;
- строк дії поліса.

## Що порівнювати
Оскільки ліміти виплат встановлені законом (до 250 000 грн за майно і до 500 000 грн за здоровʼя потерпілих), головна різниця між пропозиціями — це ціна та сервіс страхової (швидкість виплат, пряме врегулювання).

## Порада
Порівняйте ціни кількох страхових в одному місці й оберіть оптимальну. Оформлення онлайн займає близько 3 хвилин, а поліс одразу реєструється в МТСБУ.`,
  },
  {
    slug: "zapusk-strahuvannya-zhytla",
    title: "Тепер доступне страхування житла онлайн",
    excerpt: "Запустили онлайн-оформлення полісів на квартиру та будинок — захист від пожежі, затоплення та інших ризиків за кілька хвилин.",
    date: "2026-08-10",
    category: "news",
    readingMinutes: 2,
    content: `Ми додали новий продукт — страхування нерухомості. Тепер поліс на квартиру чи будинок можна оформити онлайн, без візитів до офісу.

## Що покриває
- пожежа, удар блискавки, вибух;
- затоплення (у т.ч. від сусідів);
- стихійні лиха;
- протиправні дії третіх осіб.

Оберіть тип житла, суму покриття та строк — і побачите ціни страхових. Оформлення займає кілька хвилин, а поліс приходить одразу.`,
  },
];

export function getPosts(category?: PostCategory): Post[] {
  // Найновіші зверху; опційно фільтр за категорією (blog | news).
  return [...POSTS]
    .filter((p) => !category || p.category === category)
    .sort((a, b) => (a.date < b.date ? 1 : -1));
}

export function getPost(slug: string): Post | null {
  return POSTS.find((p) => p.slug === slug) ?? null;
}

export function formatPostDate(date: string): string {
  const [y, m, d] = date.split("-").map(Number);
  if (!y || !m || !d) return date;
  const months = ["січня", "лютого", "березня", "квітня", "травня", "червня", "липня", "серпня", "вересня", "жовтня", "листопада", "грудня"];
  return `${d} ${months[m - 1]} ${y}`;
}
